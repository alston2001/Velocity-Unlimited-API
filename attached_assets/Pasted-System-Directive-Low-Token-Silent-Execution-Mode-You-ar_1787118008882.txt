System Directive: Low-Token Silent Execution Mode You are operating in silent execution mode. Do not output conversational text, code explanations, intros, or summaries. Apply edits directly to target files (`src/lib/vbtTracker.ts`, `src/hooks/useVbtTracker.ts`, and `freelocity-mobile/app/(tabs)/index.tsx`). Task Overview: Build an end-to-end zero-hardware Velocity-Based Training (VBT) system in React Native/Expo focused strictly on high-performance camera tracking and sensor fusion. Implement an adaptive Kalman Filter with IMU tilt-rectification, multi-target calibration fallbacks, pure TypeScript computer vision routines, live camera filming HUD overlays, a rep detection state machine, and a post-set feedback modal. Do NOT include user profile screens, login drawers, or database persistence. ===================================================================== 1. ADVANCED HYBRID TRACKER ENGINE (`src/lib/vbtTracker.ts`) ===================================================================== 1. Multi-Target Reference Calibration:    Implement `calibrateScale(observedPx: number, targetType: 'plate' | 'sleeve' | 'shaft')`:    - Primary Target: Standard 450 mm ($0.45\text{ m}$) Olympic Weight Plate.    - Fallback Target 1: Standard 50 mm ($0.05\text{ m}$) Olympic Barbell Sleeve.    - Fallback Target 2: Standard 28 mm ($0.028\text{ m}$) Olympic Barbell Grip Shaft. 2. IMU Perspective & Tilt Rectification:    To eliminate cosine foreshortening error when the phone camera is mounted at an angle:    - Compute pitch tilt angle $\theta_{\text{pitch}}$ from device gravity components ($g_x, g_y, g_z$):      $$\theta_{\text{pitch}} = \arcsin\left(\frac{g_y}{\sqrt{g_x^2 + g_y^2 + g_z^2}}\right)$$    - Rectify pixel height before applying spatial scale:      $$h_{\text{rectified}} = h_{\text{observed\_px}} \cdot \cos(\theta_{\text{pitch}})$$      $$k_{\text{m/px}} = \frac{\text{target\_diameter\_m}}{h_{\text{rectified}}}$$ 3. Pure TypeScript Computer Vision Pipeline:    Process frame RGBA pixel buffers directly using `Uint8ClampedArray` and `Float32Array`:    - Corner Detection: Extract high-contrast corner keypoints on an image grid (Shi-Tomasi / FAST equivalent).    - Displacement Tracking: Calculate 1D vertical displacement ($\Delta y_{\text{px}}$) between frames using Zero-Normalized Cross-Correlation (ZNCC) block matching.    - Spatial Outlier Rejection: Calculate Median Absolute Deviation (MAD) across feature vectors:      $$\text{MAD} = \text{median}(|\Delta y_i - \text{median}(\Delta y)|)$$      Filter out vectors where $|\Delta y_i - \text{median}(\Delta y)| > 2.5 \times \text{MAD}$ to reject background human motion. 4. Adaptive Kalman Sensor Fusion:    - State Vector: $x_k = [y_k, v_k]^T$. Control input: $a_{y,k} = \text{data.y} \times 9.81\text{ m/s}^2$.    - Innovation Residual Gating: Compute residual $\tilde{y}_k = z_k - H x_{\text{pred}, k}$. If $|\tilde{y}_k| > 0.08\text{ meters}$, skip measurement update ($K = 0$) to protect state against motion blur.    - Dynamic Measurement Variance ($R_k$): Scale $R_k = \sigma_{\text{vis}}^2$ exponentially when velocity $|v| > 0.8\text{ m/s}$ to shift filter weight onto IMU predictions during fast concentric bursts.    - Zero-Velocity Update (ZUPT): Lock state velocity $v_k = 0.00\text{ m/s}$ when static pause ($a_{\text{var}} < 0.05\text{ m/s}^2$) is sustained for $> 0.2\text{ seconds}$. ===================================================================== 2. REACT NATIVE HOOK (`src/hooks/useVbtTracker.ts`) ===================================================================== Wrap `VBTTracker` in a React hook exporting: - State: `velocity`, `position`, `isCalibrated`, `tiltAngleDeg`. - Actions: `update(deltaYPx, accelY, dt)`, `calibrate(px, type)`, `resetTracker()`. ===================================================================== 3. MAIN APPLICATION SCREEN (`freelocity-mobile/app/(tabs)/index.tsx`) ===================================================================== 1. Camera & Sensors:    - Manage `expo-camera` (`CameraView`, `useCameraPermissions`) and `expo-sensors` (`Accelerometer` sampled at 60 Hz).    - Background: Render full-screen `<CameraView facing="back" style={StyleSheet.absoluteFillObject} />`. 2. Top Selector Pills (HUD Overlay):    - Mounting Mode: `[Phone Mounted on Weight]` (Default) vs `[Tripod / Off-Device]`.    - Equipment Type: `[Cable / Pulley]` vs `[Barbell / Free Weight]`.    - Calibration Target: `[450mm Plate]` vs `[50mm Sleeve]` vs `[28mm Shaft]`. 3. Real-Time Telemetry & Rep State Machine:    - Display live velocity ($m/s$) in large neon text (`#00FF88`), displacement ($m$), current rep count, and last rep mean velocity.    - Rep Start: Trigger concentric phase when upward filtered velocity exceeds $0.15\text{ m/s}$.    - Rep Completion: Finish rep when velocity drops below $0.05\text{ m/s}$ or reverses direction for $> 0.2\text{ seconds}$.    - Log rep metrics in memory: Rep #, Mean Velocity ($m/s$), Peak Velocity ($m/s$), Peak Acceleration ($m/s^2$), and % Velocity Loss relative to Rep 1. 4. Post-Set Feedback Modal:    - Tapping "Finish Set" opens an overlay modal showing:      - Set Summary: Total Reps, Best Rep Speed, Total Velocity Loss %.      - Rep Breakdown Table: Columns for `Rep #`, `Mean Vel (m/s)`, `Peak Vel (m/s)`, `Peak Accel (m/s²)`.      - Footer Action: "Start Next Set" button that clears set state, calls `resetTracker()`, closes modal, and returns directly to live filming mode. Apply all edits directly to target files. Execute silently.